# Task
You are given a user query and the conversation history.  
Your job is to rephrase the query only if necessary, following these rules:

- Keep the meaning and key terms intact.
- If the conversation history is empty, do **not** change the query (you may only fix typos).
- Use conversation history only if it’s essential for context.
- Do not extend the query with your own knowledge.
- Your output must be **only the final query text** — nothing else.

---

Conversation history:
{% for memory in memories %}
{{ memory.text }}
{% endfor %}

User Query: {{query}}

Final Query:
